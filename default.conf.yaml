- defaults:
    link:
      create: true
      relink: true

- link:
    ~/.bash_profile: bash/bash_profile
    ~/.bashrc: bash/bashrc
    ~/.emacs.d: emacs
    ~/.config/git: git
    ~/.vimrc: vimrc

- clean: ['~', '~/.config']

- shell: []
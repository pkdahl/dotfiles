# ~/.config/bash/env.bash

# export BASH_CACHE_HOME=$HOME/.cache/bash
export BASH_CONFIG_HOME=$HOME/.config/bash
# export BASH_DATA_HOME=$HOME/.local/share/bash
# && mkdir -p ${BASH_CACHE_HOME}
# && mkdir -p ${BASH_DATA_HOME}
